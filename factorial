// C program for factorial 

#include <stdio.h>

int main() {
  int a , i , fact =1 ;
  printf(" enter a number for factorial : ");
  scanf("%d" , &a) ;
  
  for (i=1; i<=a ; i++)
  {
      fact = fact*i;
     
  }
  printf("  factorial for number is: %d " , fact);
    return 0;
}
